export type PlayerPointsType = {
    eventId: string;
    teamId: string;
    playerId: string;
    points: number;
}

export type PlayerInfoType = {
    teamId: string;
    playerId: string;
    firstName: string;
    lastName: string;
    displayName: string;
    shortName: string;
}

